# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── ML Inference ─────────────────────────────────────────────
onnxruntime==1.17.3          # CPU inference for ONNX model
torch==2.2.2                 # Fallback if ONNX model not yet trained
torchvision==0.17.2          # MobileNetV3 pretrained weights

# ── Image processing ─────────────────────────────────────────
Pillow==10.3.0
numpy==1.26.4

# ── Database ─────────────────────────────────────────────────
# sqlite3 is part of Python stdlib — no install needed

# ── Training (model/train.py — run on Colab) ─────────────────
# pandas==2.2.1
# pyarrow==15.0.2
# scikit-learn==1.4.2
# lightgbm==4.3.0             # routing engine (production upgrade)

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
